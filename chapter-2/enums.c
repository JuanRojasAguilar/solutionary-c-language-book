/* This file is only to refreshen how enums work, nothing fancy */

enum boolean { NO, YES }; // 0, 1

// This will set JAN to 1 and FEB as 2, and so on.
enum months {
  JAN = 1,
  FEB,
  MAR,
  APR
};
